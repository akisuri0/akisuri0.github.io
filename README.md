# akisuri0.github.io

## Learning Git

### Committing 101

`git add (file)` or `git add --all`

`git restore --staged (file)`

`git status`

`git diff`

`git commit -m (message)`

`git diff --staged`

`git push`

`git fetch`

`git pull`

`git log`

`git checkout (branch)`
